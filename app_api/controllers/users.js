const usersGetOne = (req, res) => {
  res.status(200).json({ status: "success" });
};

const usersUpdateOne = (req, res) => {
  res.status(200).json({ status: "success" });
};

const usersDeleteOne = (req, res) => {
  res.status(200).json({ status: "success" });
};

module.exports = {
  usersGetOne,
  usersUpdateOne,
  usersDeleteOne
};
